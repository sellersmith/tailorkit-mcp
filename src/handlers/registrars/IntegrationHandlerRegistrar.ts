import { ServiceHandlerRegistrar } from "./ServiceHandlerRegistrar.js";

/**
 * Integration service handler registrar
 */
export class IntegrationHandlerRegistrar extends ServiceHandlerRegistrar {
  registerHandlers(): void {
    // Register integration-specific handlers when they become available
  }
}
